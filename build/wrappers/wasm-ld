#!/bin/bash
# Wrapper to make emcc look like ld for kernel build system
# Handle --version flag specially for kernel build system checks
if [[ "$1" == "--version" ]]; then
    echo "LLD 18.0.0"
    exit 0
fi
exec emcc "$@"
